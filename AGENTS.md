# AGENTS.md

本文件是《韭菜保卫战：大A生存局》微信小游戏 MVP 的项目总控准则。后续所有 Codex 开发任务都应先阅读并遵守本文件。

## 项目定位

《韭菜保卫战：大A生存局》是一款抽象大A隐藏身份生存微信小游戏。

8 名玩家进入一间虚构交易房，其中 2 人是隐藏主力，6 人是韭菜身份。玩家经历集合竞价骗炮、分时弹幕、涨跌停炸板、T+1 锁仓、监管小黑屋等随机盘面事件，在互相博弈、保卫本金值、限制带节奏玩家和追求收益率排名中求生。

游戏核心体验是收益率竞争、社交博弈、局内资源管理、随机事件应对和火中取栗。所有金融相关表达都必须保持虚构、娱乐、模拟属性。

## 合规边界

必须严格遵守以下边界：

1. 不接入真实股票行情。
2. 不使用真实股票代码。
3. 不使用真实上市公司名称。
4. 不提供投资建议。
5. 不做真钱下注、提现、房卡、金币交易、赔率、庄家、实物兑奖。
6. 所有“本金值、信心值、操盘点、收割值”都只是局内虚拟参数，无任何现金价值。
7. 游戏文案必须明确“虚构娱乐模拟”。

实现、测试、文案、配置、示例数据和注释中都不能绕开以上边界。若需要股票、指数、公司、新闻、盘口等素材，必须使用完全虚构名称和虚构数值。

## 技术路线

- 客户端：Cocos Creator 3.8+，TypeScript。
- 服务端：Node.js + TypeScript + WebSocket。
- 共享包：`packages/shared`，存放类型、规则、算法、常量。
- 第一版不接数据库，房间状态保存在内存。
- 第一版不接支付、不接登录、不接广告 SDK。
- 第一版用文字模拟配音，不要求真实音频。

## 仓库组织原则

推荐采用 monorepo 结构，客户端、服务端和共享规则分离：

```text
.
├── AGENTS.md
├── package.json
├── tsconfig.base.json
├── apps
│   ├── client-cocos
│   │   ├── assets
│   │   ├── settings
│   │   └── package.json
│   └── server
│       ├── src
│       ├── tests
│       └── package.json
└── packages
    └── shared
        ├── src
        │   ├── constants
        │   ├── events
        │   ├── protocol
        │   ├── rules
        │   ├── types
        │   └── utils
        ├── tests
        └── package.json
```

## 代码约束

- 所有关键结算必须由服务端完成，客户端只展示状态并提交动作。
- 客户端不得自行判定胜负、阵营结果、随机事件结果或关键资源结算。
- `packages/shared` 不能依赖 Cocos、DOM、Node 专属 API。
- 所有随机逻辑必须可测试，优先封装为纯函数。
- 需要随机数时，优先通过可注入 RNG 或 seed 派生函数实现，避免在核心规则中直接调用 `Math.random()`。
- 每个阶段必须尽量写 Vitest 测试。
- 不要引入重型框架。
- 不要使用 Socket.IO，使用原生 WebSocket JSON 协议。
- 网络协议应使用明确的 `type` 字段和结构化 payload，避免字符串拼接协议。
- 服务端收到客户端动作后必须校验房间、玩家、阶段、权限和参数合法性。
- Bot 逻辑应走与真人玩家尽量一致的动作入口，避免绕过规则。

## 第一版核心功能

MVP 第一版目标功能：

1. 8 人房。
2. 2 隐藏主力 + 6 韭菜。
3. Bot 补位。
4. 5 个交易日。
5. 集合竞价可撤/锁单。
6. 分时弹幕。
7. 涨跌停、炸板。
8. T+1 锁仓。
9. 监管热度和小黑屋问询。
10. 龙虎榜投票。
11. ROI 收益率排名、结算称号和复盘分享。

## 胜负目标

- 游戏不再以“抓出主力”作为主要胜负目标。
- 所有玩家，无论主力还是韭菜，最终都按收益率 ROI 排名。
- ROI 公式：`(最终资金 - 初始资金) / 初始资金`。
- 第 5 个交易日结束后，ROI 第一名是本局冠军。
- 主力身份和韭菜身份只是不同的赚钱方式，不是直接胜负阵营。
- 主力被投票关注或监管问询命中，只会造成操盘能力、弹幕资源或监管压力变化，不直接判负。
- 韭菜投票命中主力不直接胜利，只是限制主力影响力。
- 标准模式初始资金：主力 `1000`，韭菜 `100`。

## 投票机制

- 集合竞价情绪投票：投看多、看空、观望，影响开盘情绪和盘口权重。
- 龙虎榜关注投票：每日收盘后投“谁今天最像带节奏的人”，票数最高者进入关注。
- 监管问询投票：监管热度过高时投“本轮异常波动主要是谁造成的”，投中者次日受限。
- 尾盘方向投票：收盘前投封板、炸板、跳水、回拉，影响尾盘走势概率。
- 投票是市场工具，不是抓人即胜利机制。

## 游戏规则实现原则

- 房间状态应有清晰阶段机：等待、身份分配、交易日开始、集合竞价、分时阶段、事件结算、投票、交易日结束、最终结算。
- 交易日总数第一版固定为 5 天。
- 玩家数量第一版固定为 8 人，不足时用 Bot 补齐。
- 身份分配由服务端完成，隐藏主力身份只发给本人，其他玩家只能收到必要的公开信息。
- 标准模式固定 2 个主力和 6 个韭菜。
- 最终结算必须以 ROI 排名为主榜，绝对收益榜、主力操盘效率榜、韭菜火中取栗榜和沙雕称号榜是辅助榜单。
- 股票走势必须由服务端调用 `packages/shared` 中的多因子虚构行情引擎生成，不能由主力或任一玩家完全控制。
- 行情因子应包含匿名化/合成走势模板、五行板块状态、主力操作、玩家交易拥挤度、弹幕情绪、系统量化机构、监管因素、背景市场资金和随机扰动。
- 若未来使用真实历史数据，只能作为匿名化、归一化、扰动后的走势模板，不得保存或展示真实股票名、股票代码、真实交易日期或可反推真实行情的信息。
- 背景市场资金应包含 passiveLiquidity、npcRetailFlow、hotMoneyFlow、sectorRotationFlow、quantFlow，使没有玩家交易时股票也可能产生虚构涨跌。
- 所有资源字段都应明确为局内虚拟参数，例如本金值、信心值、操盘点、收割值。
- 监管热度、小黑屋问询、涨跌停、炸板等事件必须使用虚构娱乐表达，不影射真实市场或真实主体。
- 结算逻辑应优先放在 `packages/shared` 的纯函数中，由服务端调用。

## 测试要求

- 使用 Vitest 编写单元测试。
- 优先覆盖 shared 包中的纯规则函数。
- 关键测试场景包括身份分配、Bot 补位、阶段流转、集合竞价锁单/撤单、T+1 锁仓、随机事件确定性、投票限制效果、最终 ROI 收益率排名。
- 随机相关测试必须能通过固定 seed 或注入 RNG 复现。
- 服务端测试应覆盖非法动作、越阶段动作、重复动作和断线/重连可恢复状态。

## 文案规范

- 所有对外展示文案必须体现“虚构娱乐模拟”。
- 避免真实投资术语的指导性表达，例如“建议买入”“稳赚”“止盈止损策略”等。
- 可以使用夸张、抽象、喜剧化的虚构盘面词汇，但不得指向真实股票、真实代码、真实公司或真实行情。
- 示例名称应使用虚构风格，例如“葱花科技”“泡面能源”“月台股份”，且不得匹配真实上市公司名称。

## 当前阶段约束

Phase 1 开始实现 MVP 共享规则、服务端内存房间和 Cocos 展示骨架；新增核心规则前仍需先同步本文件。

后续阶段开始前，应先确认当前任务是否会改变技术路线、合规边界或核心规则；如会改变，应先更新本文件，再实施代码。
