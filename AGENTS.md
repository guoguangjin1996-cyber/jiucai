# AGENTS.md

本文件是《韭菜保卫战：大A生存局》MVP 的项目总控准则。后续所有 Codex 开发任务都应先阅读并遵守本文件。

## 当前技术路线

本阶段先使用 Vue 做一版可玩的 MVP 客户端，优先验证房间、阶段、局内动作、弹幕、投票和复盘闭环。

- 客户端：Vue 3 + Vite + TypeScript，目录为 `apps/client-vue`。
- 历史客户端：`apps/client-cocos` 暂时保留为 Cocos 骨架和视觉参考，不作为当前优先实现入口。
- 服务端：Node.js + TypeScript + 原生 WebSocket JSON 协议。
- 共享包：`packages/shared`，存放类型、规则、算法、常量。
- 第一版不接数据库，房间状态保存在内存。
- 第一版不接支付、不接登录、不接广告 SDK。
- 第一版用文字模拟配音，不要求真实音频。

后续若重新回到微信小游戏引擎路线，再评估 Cocos Creator 版本、迁移范围和小程序/小游戏产品形态。

## 项目定位

《韭菜保卫战：大A生存局》是一款抽象大A隐藏身份生存游戏。玩家进入一间虚构交易房，经历集合竞价骗炮、分时弹幕、涨跌停炸板、T+1 锁仓、监管小黑屋等随机盘面事件，在互相怀疑、保卫本金和投票博弈中求生。

游戏核心体验是社交推理、局内资源管理、随机事件应对、阵营博弈，以及新手规则学习与盘面风格体验。所有金融相关表达都必须保持虚构、娱乐、模拟属性。

本游戏不是投资工具，不教用户如何赚钱，不推荐任何真实股票，不接入真实股票行情，不提供投资建议。

## 合规边界

必须严格遵守以下边界：

1. 不接入真实股票行情。
2. 不使用真实股票代码。
3. 不使用真实上市公司名称。
4. 不提供投资建议。
5. 不做真钱下注、提现、房卡、金币交易、赔率、庄家、实物兑奖。
6. 所有“本金值、信心值、操盘点、收割值、ROI、走势”都只是局内虚拟参数，无任何现金价值。
7. 游戏文案必须明确“虚构娱乐模拟”。

实现、测试、文案、配置、示例数据和注释中都不能绕开以上边界。若需要股票、指数、公司、新闻、盘口等素材，必须使用完全虚构名称和虚构数值。

## 代码约束

- 所有关键结算必须由服务端完成，客户端只展示状态并提交动作。
- 客户端不得自行判定胜负、身份结果、随机事件结果或关键资源结算。
- `packages/shared` 不能依赖 Cocos、DOM、Node 专属 API。
- 所有随机逻辑必须可测试，优先封装为纯函数。
- 需要随机数时，优先通过可注入 RNG 或 seed 派生函数实现，避免在核心规则中直接调用 `Math.random()`。
- 每个阶段必须尽量写 Vitest 测试。
- 不要引入重型框架。
- 不要使用 Socket.IO，使用原生 WebSocket JSON 协议。
- 网络协议应使用明确的 `type` 字段和结构化 payload，避免字符串拼接协议。
- 服务端收到客户端动作后必须校验房间、玩家、阶段、权限和参数合法性。
- Bot 逻辑应走与真人玩家尽量一致的动作入口，避免绕过规则。

## MVP 核心功能

1. 固定人数房间，人数不足时用 Bot 补齐。
2. 隐藏主力 + 韭菜阵营。
3. 多个交易日阶段流转。
4. 集合竞价可撤/锁单。
5. 分时弹幕。
6. 涨跌停、炸板。
7. T+1 锁仓。
8. 监管热度和小黑屋问询。
9. 龙虎榜投票。
10. 阵营结算、ROI 排名和个人称号。
11. 新手规则学习、术语图鉴和局后复盘解释。

## 游戏规则实现原则

- 房间状态应有清晰阶段机：等待、身份分配、交易日开始、集合竞价、分时阶段、事件结算、投票、交易日结束、最终结算。
- 身份分配由服务端完成，隐藏主力身份只发给本人，其他玩家只能收到必要的公开信息。
- 股票走势必须由服务端调用 `packages/shared` 中的多因子虚构行情引擎生成，不能由主力或任一玩家完全控制。
- 行情因子应包含匿名化/合成走势模板、五行板块状态、主力操作、玩家交易拥挤度、弹幕情绪、系统量化机构、监管因素、背景市场资金和随机扰动。
- 背景市场资金应包含 passiveLiquidity、npcRetailFlow、hotMoneyFlow、sectorRotationFlow、quantFlow，使没有玩家交易时股票也可能产生虚构涨跌。
- 教学模式应解释规则和风险体验，不得提供真实交易方法、真实股票推荐或盈利承诺。
- 局后复盘应解释玩家在虚构盘面中的 T+1 锁仓、集合竞价、涨跌停、板块轮动、量化拥挤、监管问询和 ROI 排名原因。
- 监管热度、小黑屋问询、涨跌停、炸板等事件必须使用虚构娱乐表达，不影射真实市场或真实主体。
- 结算逻辑应优先放在 `packages/shared` 的纯函数中，由服务端调用。

## 测试要求

- 使用 Vitest 编写单元测试。
- 优先覆盖 shared 包中的纯规则函数。
- 关键测试场景包括身份分配、Bot 补位、阶段流转、集合竞价锁单/撤单、T+1 锁仓、随机事件确定性、投票结算、最终胜负。
- 随机相关测试必须能通过固定 seed 或注入 RNG 复现。
- 服务端测试应覆盖非法动作、越阶段动作、重复动作和断线/重连可恢复状态。

## 文案规范

- 所有对外展示文案必须体现“虚构娱乐模拟”。
- 避免真实投资术语的指导性表达，例如“建议买入”“稳赚”“止盈止损策略”等。
- 可以使用夸张、抽象、喜剧化的虚构盘面词汇，但不得指向真实股票、真实代码、真实公司或真实行情。
- 示例名称应使用虚构风格，例如“葱花科技”“泡面能源”“月台股份”，且不得匹配真实上市公司名称。
